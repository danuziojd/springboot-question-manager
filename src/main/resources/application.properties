spring.application.name=ejercicio_avaluacion-final

# --- Perfil activo por defecto (se sobreescribe con SPRING_PROFILES_ACTIVE) ---
spring.profiles.active=dev

# --- JPA / Hibernate (comun a ambos perfiles) ---
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.error.whitelabel.enabled=false
spring.mvc.throw-exception-if-no-handler-found=true
server.error.include-message=always

# --- JWT ---
jwt.secret=claveSecretaParaElExamenFinalDeSpringBootMuyLargaYSegura123456